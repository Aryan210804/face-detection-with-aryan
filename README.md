# face-detection-with-aryan

1)The program loads an image, detects faces, and displays the result.
2)It uses Haar Cascades to find faces in grayscale images.
3)The code iterates through multiple images to detect faces in each.
