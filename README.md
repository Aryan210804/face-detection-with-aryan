# face-detection-with-aryan

The program loads an image, detects faces, and displays the result.
It uses Haar Cascades to find faces in grayscale images.
The code iterates through multiple images to detect faces in each.
